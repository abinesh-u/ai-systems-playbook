# Color System

Use neutral surfaces and one restrained accent palette. Color conveys category, not decoration: blue = process/service; green = data/validated state; amber = decision/caution; red = failure/blocker; gray = external/context. Never rely on color alone; label meaning and meet contrast requirements. Do not assign semantic color to a vendor or product.
