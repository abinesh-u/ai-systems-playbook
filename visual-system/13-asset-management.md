# Asset Management

Specifications: `visual-library/specifications/`; reusable templates: `templates/`; category source assets: category directories; exports: `exports/`; shared icons: `icons/`.

Search by ID, category, pillar, hub, article, status, owner, and version. Never overwrite a released visual; create a versioned successor and update references. Track license, source, and review status with each asset.
