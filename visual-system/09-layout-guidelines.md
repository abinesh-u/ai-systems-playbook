# Layout Guidelines

Start with system boundary and reading direction. Keep alignment on a grid; use equal spacing for equivalent relationships; group by ownership before data flow. Avoid crossing lines, dense labels, and duplicated nodes. Split a visual when more than one reader question or three distinct flows compete.
