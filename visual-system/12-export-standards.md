# Export Standards

Keep editable source with the specification. Preferred web export: SVG; fallback: PNG at 2× display resolution. Use PDF only for print. Optimize filenames and dimensions; do not embed sensitive data or inaccessible text. Exports go to `visual-library/exports/`; source/specification remains version-controlled.
